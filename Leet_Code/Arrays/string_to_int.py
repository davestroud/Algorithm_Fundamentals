class Solution:
    def myAtoi(self, s: str) -> int:
        # Read in and ignore whitespace

        # Check is next char is -/+ 
        # Read in if either

        # Read in next char nutil next non-dig char
        # or end is reached

        # Convert dig to int ~ if no read == 0

        # If int out of 32bit ~ clamp int

        # Return int

